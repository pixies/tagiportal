<h1>Listing clientes</h1>

<div>
  <div id="">
    <% @clientes.each do |cliente| %>
      <div>
        <div><span class="title">Nome: </span><%= cliente.nome %></div>
   <div><span class="title">Serviço(s): </span><%= cliente.servico.nome %></div>
        <div><%= link_to 'Show', cliente %></div>
        <div><%= link_to 'Edit', edit_cliente_path(cliente) %></div>
        <div><%= link_to 'Destroy', cliente, method: :delete, data: { confirm: 'Are you sure?' } %></div>
      </div>
    <% end %>
  </div>
</div>

<br>

<%= link_to 'New Cliente', new_cliente_path %>
